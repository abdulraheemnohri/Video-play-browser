<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.videoplay.browser">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- GeckoView Permissions -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Picture-in-Picture -->
    <uses-feature android:name="android.hardware.type.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".BrowserApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VIDEOPlayBrowser"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!-- Default Browser -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.VIDEOPlayBrowser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- Browser Activity -->
        <activity
            android:name=".ui.BrowserActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.VIDEOPlayBrowser" />

        <!-- Video Player Activity -->
        <activity
            android:name=".video.playback.VideoPlayerActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.VIDEOPlayBrowser.NoActionBar"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|uiMode|isLayoutDirectionChanged|keyboard|keyboardHidden|navigation|mnc|density|fontScale|screenLayout|orientation|uiMode"
            android:windowSoftInputMode="adjustResize" />

        <!-- Fullscreen Video Activity -->
        <activity
            android:name=".video.fullscreen.FullscreenVideoActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.VIDEOPlayBrowser.Fullscreen"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|uiMode|isLayoutDirectionChanged|keyboard|keyboardHidden|navigation|mnc|density|fontScale" />

        <!-- Settings Activity -->
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.VIDEOPlayBrowser" />

        <!-- Service for Downloads -->
        <service
            android:name=".downloads.DownloadService"
            android:exported="false" />

        <!-- Service for MediaSession -->
        <service
            android:name=".video.media.MediaSessionService"
            android:exported="false" />

        <!-- Receiver for Notifications -->
        <receiver
            android:name=".downloads.DownloadReceiver"
            android:exported="false" />

        <!-- App Shortcuts -->
        <meta-data
            android:name="android.app.android.shortcuts"
            android:resource="@xml/shortcuts" />

        <!-- Default Browser -->
        <meta-data
            android:name="android.app.default_browser"
            android:value="true" />
    </application>
</manifest>
